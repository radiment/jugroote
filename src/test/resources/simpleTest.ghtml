<!DOCTYPE html>
<html>
if (binding.hasVariable("body") || binding.hasVariable("<test>")) {
    <body
    class="${body}">
        if (body.length > 0 && body.length < 15) {
            ${body}
            ${"test{}</test>"}
        }
    </body>
}
</html>